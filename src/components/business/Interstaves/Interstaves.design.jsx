import React from 'react';
import classes from './Interstaves.module.scss';

const Interstaves = () => (
  <p className={classes.interStaves}>
    <span className={classes.startBarInterStaves}>𝄀</span>
    <span className={classes.endBarInterStaves}>𝄂</span>
  </p>
);

export default Interstaves;
